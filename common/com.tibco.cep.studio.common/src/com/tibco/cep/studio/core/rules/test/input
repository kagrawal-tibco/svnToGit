/**
 * @description 
 * @author hydra-THINK
 */
rule Rules.RuleHandleTestEvent {
	attribute {
		priority = 5;
		forwardChain = true;
	}
	declare {
		
		Events.Test test;
	}
	when {
		
	}
	then {
	   System.debugOut("Testing!!");
       System.debugOut("Printing Chiness property:" + test.一二三);
       System.debugOut("Printing Korean property:" + test.한글);
       System.debugOut("Printing Janpaness property:" + test.おき);
	}
}