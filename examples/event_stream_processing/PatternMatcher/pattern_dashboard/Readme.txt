========================================================================
Instructions
========================================================================
To run the Pattern Dashboard from the command line, go to the folder
containing pattern_dashboard.jar and type the following:

java -jar "pattern_dashboard.jar"

Ensure that the Java Runtime used by this is the same or newer than the
one used by the Pattern Server.

========================================================================