/**
 * @description 
 * @author 
 */
rule Rules.Normal.Start {
	attribute {
		priority = 5;
		forwardChain = true;
	}
	declare {
		Model.StartTest	starttest;
		
	}
	when {
		
	}
	then {
		Model.Context.status = "start";
		Model.Context.firstGuestName = starttest.firstGuest;
		Model.Context.numGuest = starttest.numGuest;
	}
}