/**
 * @description 
 * @author 
 */
rule Rules.Normal.Reset {
	attribute {
		priority = 3;
		forwardChain = true;
	}
	declare {
		Model.ResetTest	resettest;
		Model.Seat	seat;
		
	}
	when {
		
	}
	then {
		Instance.deleteInstance(seat);
	}
}