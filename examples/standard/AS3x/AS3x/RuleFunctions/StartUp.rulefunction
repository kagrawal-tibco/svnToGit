/**
 * @description 
 */
void rulefunction RuleFunctions.StartUp {
	attribute {
		validity = ACTION;
	}
	scope {
		
	}
	body {
		System.debugOut("Initializing and setting up connections with the store.");
	
		String asURL = "http://localhost:5055";
		Object storeProperties = Store.ConnectionInfo.create(asURL, "AS"); 
		Store.connect(storeProperties);
	}
}