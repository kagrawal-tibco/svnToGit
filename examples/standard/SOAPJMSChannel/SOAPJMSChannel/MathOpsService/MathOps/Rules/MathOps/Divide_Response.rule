/**
 * @description 
 * @author WsdlImport
 */
rule MathOpsService.MathOps.Rules.MathOps.Divide_Response {
	attribute {
		priority = 5;
		forwardChain = true;
	}
	declare {
		MathOpsService.MathOps.Events.DivideResponseMsg	response;
		
	}
	when {
		
	}
	then {
		Log.log(Log.getLogger("SOAP"), "info", "Response %s to operation: Divide", response@id);// TODO - implement
		Event.consumeEvent(response);
	}
}