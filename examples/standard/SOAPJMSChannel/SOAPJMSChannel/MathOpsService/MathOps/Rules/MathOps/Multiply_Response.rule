/**
 * @description 
 * @author WsdlImport
 */
rule MathOpsService.MathOps.Rules.MathOps.Multiply_Response {
	attribute {
		priority = 5;
		forwardChain = true;
	}
	declare {
		MathOpsService.MathOps.Events.MultiplyResponseMsg	response;
		
	}
	when {
		
	}
	then {
		Log.log(Log.getLogger("SOAP"), "info", "Response %s to operation: Multiply", response@id);// TODO - implement
		Event.consumeEvent(response);
	}
}