/**
 * @description 
 * @author WsdlImport
 */
rule MathOpsService.MathOps.Rules.MathOps.Subtract_Response {
	attribute {
		priority = 5;
		forwardChain = true;
	}
	declare {
		MathOpsService.MathOps.Events.SubtractResponseMsg	response;
		
	}
	when {
		
	}
	then {
		Log.log(Log.getLogger("SOAP"), "info", "Response %s to operation: Subtract", response@id);// TODO - implement
		Event.consumeEvent(response);
	}
}