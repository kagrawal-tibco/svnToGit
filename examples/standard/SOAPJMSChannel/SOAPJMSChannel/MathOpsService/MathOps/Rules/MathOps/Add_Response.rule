/**
 * @description 
 * @author WsdlImport
 */
rule MathOpsService.MathOps.Rules.MathOps.Add_Response {
	attribute {
		priority = 5;
		forwardChain = true;
	}
	declare {
		MathOpsService.MathOps.Events.AddResponseMsg	response;
		
	}
	when {
		
	}
	then {
		Log.log(Log.getLogger("SOAP"), "info", "Response %s to operation: Add", response@id);// TODO - implement
		Event.consumeEvent(response);
	}
}