/**
 * @description 
 * @author 
 */
rule Rules.LargeOrders.ProcessLargeOrder {
	attribute {
		priority = 3;
		forwardChain = true;
	}
	declare {
		Events.OrderEvent	order;
		
	}
	when {
		
	}
	then {
		System.debugOut("Processing the LARGE order (" + order.Amount + ") " + order.Description);
	}
}