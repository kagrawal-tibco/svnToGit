/**
 * @description 
 * @author 
 */
rule Rules.SmallOrder.ProcessSmallOrder {
	attribute {
		priority = 3;
		forwardChain = true;
	}
	declare {
		Events.OrderEvent	order;
		
	}
	when {
		
	}
	then {
		System.debugOut("Processing the SMALL order (" + order.Amount + ") " + order.Description);
	}
}